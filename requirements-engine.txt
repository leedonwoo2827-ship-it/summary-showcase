# 엔진 venv(.venv) — TTS/렌더. 콘솔(.venv-app)과 분리한다.
# onnxruntime 이 콘솔 의존성과 충돌하고, 렌더 중에도 콘솔을 닫을 수 있어야 한다.
# 두 쪽은 파일로만 대화한다.
onnxruntime>=1.17     # 또는 onnxruntime-gpu — 정확히 하나만 설치
numpy>=1.26
soundfile>=0.12.1
pyyaml>=6.0
pysrt>=1.1
Pillow>=10
